---
title: "FAQs"
description: "Questions about timelines, content, SEO campaigns, support, and what it is like to build and grow a website with us."
heading: "Common Questions Before We Build"
hasPage: true
itemsHasPage: false
addToMenu:
  - menu: "main-menu"
    order: 5
  - menu: "footer-menu"
    order: 5
---
import ContentRenderer from "@/components/ContentRenderer/ContentRenderer.astro";
import { query, sortByOrder } from "@/utils/query";

<ContentRenderer
  query={query("faq").orderBy(sortByOrder())}
  variant="AccordionVariant"
  title={frontmatter.title}
  heading={frontmatter.heading}
  description={frontmatter.description}
  allowMultiple={false}
  showButtonSection={false}
/>
