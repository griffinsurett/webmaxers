---
title: "Projects"
description: "Selected sites and campaigns built for our clients."
heading: "See how we turn ideas into high-performing sites."
hasPage: true
itemsHasPage: false
addToMenu:
  - menu: "main-menu"
    order: 4
  - menu: "footer-menu"
    order: 4
  - menu: "footer-menu-2"
  - menu: "links-menu"
    order: 2
    icon: "fa6-solid:briefcase"
---
import ContentRenderer from '@/components/ContentRenderer/ContentRenderer.astro';
import { query, sortByOrder } from '@/utils/query';
import CollectionBodyLayout from "@/layouts/collections/CollectionBodyLayout.astro";

<CollectionBodyLayout
  title={frontmatter.title}
  heading={frontmatter.heading}
  description={frontmatter.description}
>
  <ContentRenderer
    query={query('projects').orderBy(sortByOrder())}
    variant="PortfolioVideoGridShowcaseVariant"
    maxItems={999}
    showMetadata={true}
  />
</CollectionBodyLayout>
