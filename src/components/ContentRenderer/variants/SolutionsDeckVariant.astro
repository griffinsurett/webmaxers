---
// src/components/ContentRenderer/variants/SolutionsDeckVariant.astro
import type { BaseVariantProps } from "../ContentRenderer.types";
import Icon from "@/components/Icon";

type Props = BaseVariantProps;

const { items = [], id, className = "" } = Astro.props as Props;

// Shared card padding — applied to the top row and the body so the card itself
// has no padding and the middle (future-content slot) can run edge-to-edge.
const PAD = "p-[clamp(1.5rem,2.2vw,2rem)]";
---

<section
  id={id}
  class:list={[
    "section-container flex flex-col justify-center min-h-[100svh] py-[clamp(4rem,8vh,6rem)]",
    className,
  ]}
  aria-label="Our solutions"
>
  <ul class="list-none grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-5 m-0 p-0">
    {
      items.map((item) => {
        const Tag = item.url ? "a" : "div";
        return (
          <li>
            <Tag
              {...(item.url ? { href: item.url } : {})}
              class="group relative flex flex-col h-full min-h-[clamp(22rem,26vw,30rem)] rounded-lg overflow-hidden no-underline bg-bg3 border border-heading/8 transition-[border-color,transform] duration-200 hover:border-heading/18 hover:-translate-y-0.5"
            >
              {/* Top row: icon + title (left) inline with the circular arrow (right) */}
              <div class={`flex items-center justify-between gap-4 ${PAD} pb-0`}>
                <div class="flex items-center gap-3 min-w-0">
                  {item.icon && (
                    <span class="inline-flex shrink-0 text-heading/45">
                      <Icon icon={item.icon} size="lg" />
                    </span>
                  )}
                  <h3 class="text-[clamp(1.25rem,1.6vw,1.5rem)] font-normal leading-[1.1] tracking-[-0.02em] text-heading">
                    {item.title}
                  </h3>
                </div>
                <span
                  class="inline-flex items-center justify-center shrink-0 w-7 h-7 rounded-full text-heading/35 border border-heading/10 transition-[color,border-color,transform] duration-200 group-hover:text-heading group-hover:border-heading/30 group-hover:translate-x-0.5 group-hover:-translate-y-0.5"
                  aria-hidden="true"
                >
                  <svg viewBox="0 0 24 24" fill="none" width="15" height="15">
                    <path
                      d="M7 17L17 7M17 7H8M17 7V16"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    />
                  </svg>
                </span>
              </div>

              {/* Description + future-content slot pushed to bottom */}
              <div class={`flex flex-col gap-2.5 mt-auto ${PAD} pt-0`}>
                {/* Placeholder for future content — intentionally empty/hidden
                    for now. Reserve the slot above the description. */}
                <div hidden aria-hidden="true"></div>

                {item.description && (
                  <p class="text-[clamp(0.875rem,1vw,0.9375rem)] font-light leading-[1.7] text-heading/42">
                    {item.description}
                  </p>
                )}
              </div>
            </Tag>
          </li>
        );
      })
    }
  </ul>
</section>
