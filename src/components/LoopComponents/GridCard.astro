---
// src/components/LoopComponents/GridCard.astro
/**
 * Grid Card Component
 *
 * Simple card for grid layouts with:
 * - Optional image
 * - Title (linked or plain)
 * - Description
 * - Subtle hover effects
 *
 * Generic card used in GridVariant - the default section variant.
 */

import Icon from "@/components/Icon";

export interface Props {
  title: string;
  url?: string;
  description?: string;
  image?: {
    src: string;
    alt: string;
  };
  icon?: any;
}

const { title, url, description, image, icon } = Astro.props;
---

<article
  class="relative bg-bg rounded-lg overflow-hidden shadow-md hover:shadow-lg hover:-translate-y-1 transition-all duration-200"
>
  {/* Optional image */}
  {
    image && (
      <img src={image.src} alt={image.alt} class="w-full h-48 object-cover" />
    )
  }

  {/* Card content */}
  <div class="p-6">
    {icon && (
      <div class="mb-4 w-10 h-10 flex items-center justify-center rounded-lg bg-white/5">
        <Icon icon={icon} size="lg" className="text-heading" />
      </div>
    )}
    <h3 class="text-xl font-semibold mb-2">
      {
        url ? (
          <a
            href={url}
            class="text-heading hover:text-primary transition-colors after:absolute after:inset-0 after:content-[''] z-10"
          >
            {title}
          </a>
        ) : (
          <span class="text-heading">{title}</span>
        )
      }
    </h3>

    {description && <p class="text-text leading-relaxed">{description}</p>}
  </div>
</article>
